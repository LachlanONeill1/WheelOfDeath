@using LibWheelOfDeath
@using Assessment
@using System.Data.SqlClient;
@using LibEntity.NetFramework
@using System.Data;
@{
    LibWebWheelOfDeath.Model.Initialize();

    CAdmin admin = (CAdmin)Session["LoggedAdmin"] ?? new CAdmin();
    Session["LoggedAdmin"] = admin;
}